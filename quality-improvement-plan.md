# 重建质量提升方案:清晰度 × 完整度(流程锁定)

> 目标:**在现有流水线(上传 → 视图筛选 → ZipSplat 重建 → 去噪 → 导出)结构完全不变的前提下**,
> 找到提升重建"清晰度"(锐度、色彩干净度)与"完整度"(几何无洞、物体不缺块、不多漂浮物)的方法。
> 所有改动限于:筛选参数、推理调用参数、去噪后处理阈值。流程结构不动。

---

## 0. 决定方法空间的核心事实(已核实源码)

| 事实 | 含义 |
|---|---|
| 模型固定 **252×252** 输入(`to_square` 中心裁剪 + LANCZOS 缩放,`utils.py:220-225`) | **输入分辨率不是杠杆**。喂 4K 与喂 640 效果相同。但"中心裁剪"是杠杆:主体偏边会被裁掉 → 完整度损失 |
| `compression=1.0` = 禁用 k-means 采样,每 token 全量解码 | 已是最高质量档,**无上调空间** |
| 仅一个权重 `zipsplat-da3g-252p`(DA3-Giant,RE10K+DL3DV 混合训练) | 无"更高清权重"可选 |
| 官方评测(DL3DV, pose-free):**6 views 25.24 > 12 views 24.27 > 24 views 24.14** (PSNR) | **视图越多清晰度越降**(252p 下注意力被稀释);但覆盖/完整度上升。6-12 是甜点区 |
| 官方评测(priors vs pose-free):**RE10K 6v +1.0 PSNR** (26.20→27.19)、MipNeRF360 32v +1.2、DL3DV 12v 仅 +0.1 | **位姿先验对物体/小场景(object 模式)收益大,对大广角场景几乎无收益** |
| `use_priors=True` 需要 `(V,3,H,W)` tensor 输入 + 批量 Camera/Pose;pose-free 用学习到的默认相机 token | priors 接入是推理调用方式变化,**不是流程变化** |
| `runner.py:55` 调 `select_best_views(input_dir, num=num_views)` **不带 out_dir** → SfM 位姿算出即丢 | 接 priors 只需把位姿传进来(pick_views 原生支持导出 `poses.npy/cameras.npy`,splatfactory 格式,内参会自动适配 252p 缩放) |

---

## 1. 杠杆清单(按预期收益排序)

### 1.1 输入侧(零风险,先做)

- **I1 视图数量**:当前 `num_views=12`。官方数据支持 6-12 甜点。实验 {6, 8, 12, 16} 找本场景拐点。
  注意 >12 张上传才触发选择;≤12 全量入模。
- **I2 曝光一致性(最重要但最易被忽略)**:多图自动曝光差异 → 模型在 252p 下做色彩折中 → **糊+变色**。
  当前仅极端阈值过滤(灰度均值 <30 或 >225 剔除,`pick_views.py:56-61`),不做一致性处理。
  方案:喂模型前按灰度中位数做线性增益对齐(标准做法,数行代码)。
- **I3 模糊阈值收紧**:`blur_factor=0.4`(中位数 Laplacian 的 40%)→ 试 0.5/0.6,更严剔除失焦图。
- **I4 视角最小间距**:`min_gap_deg=8°` → 12-15°。减少近冗余视角,让每个 token 吃到独立信息。
- **I5 中心裁剪取景(记录即可,不动)**:竖屏图中心裁剪会切主体;依赖 SfM 贪心选视角缓解。

### 1.2 模型侧(中杠杆,需对照实验)

- **M1 use_priors 接入**:SfM 位姿喂入模型(`use_priors=True`)。
  - 预期:**object 模式(接近 RE10K 分布)+1 PSNR 量级**;scene 模式收益小(参考 DL3DV)。
  - 实施:runner 拿 `select_best_views` 返回的 poses → 构造 `Camera.from_K`(splatfactory cameras.npy 格式)+ `Pose` → 以 tensor 输入调 `model(images_t, cameras, poses, use_priors=True)`。
  - **风险**:SfM 位姿有噪声(单目尺度漂移),错误位姿引导 attention 反而更差 → 必须对照实验,注册率高才启用,失败回退 pose-free。
  - 备选:仅内参(fov)从 EXIF 估算喂入 —— predictor 要求 poses 同传,无 pose 则不可行,跳过。

### 1.3 后处理侧(确定性交付,在指标上回归)

- **P1 大 scale 高斯截断(当前缺失)**:超大 scale 的高斯 = 雾状残渣,是"糊感"主来源之一。
  DBSCAN/透明度过滤删不掉"贴在物体上的大球"。方案:按场景尺度分位数截断(如删 `scale > q99` 或按对角线的比例),实验阈值。
- **P2 DBSCAN top_n 3 → 5**:当前 object 模式只保前 3 大簇,**多物体场景会整体删掉小物体**(完整度损失)。
  3→5 后小物体保留;与"剔背景"目标权衡,看保留率与目检。
- **P3 透明度阈值微调**:object `alpha_threshold=0.05` / scene `0.02`。太紧删细节,太松留雾。小范围实验。

### 1.4 明确不做的(防止浪费)

- 输入分辨率提升:模型 252p 固定,无效。
- compression < 1.0:只会降质量。
- 换权重/微调模型:无权重可选,训练不在本次范围。
- 流程结构改动(worker/队列/产物):用户已锁定。

---

## 2. 评估闭环(先建,否则所有调参都是盲的)

> 上一轮语义方向失败的直接教训:**没有客观指标,目检无法区分"变好"与"碰巧"**。

- **E1 留出视角 NVS(主指标)**:`select_best_views` 的 SfM 注册图中留 2 张**不喂模型**;
  重建后在这些位姿上渲染(gsplat,复用 semantics 的渲染路径),与真实图比:
  - **PSNR**(手写,几行)、**SSIM**(手写高斯窗口,不装 torchmetrics/lpips,离线可用)
  - 输出对比目检图(左右拼图)。
- **E2 渲染锐度统计(副指标,无需 GT)**:多视角渲染图的 Laplacian 方差均值。
- **E3 完整度代理**:高斯总数、去噪保留率、DBSCAN 簇数 —— 记录在案,防"删光变干净"的假象。

每个实验配置跑完输出一张指标卡(PSNR/SSIM/锐度/高斯数),与基线并排。

---

## 3. 里程碑

| 里程碑 | 内容 | 验证 |
|---|---|---|
| **M0 评估基线** | 留出 NVS 脚本(E1/E2/E3)+ 当前参数跑通,记录基线分数 | 客观指标锁定起点 |
| **M1 输入侧实验** | I1 视图数 {6,8,12,16} × I2 曝光对齐 {off,on} × I3/I4 收紧,少量组合 | E1 分数对比,挑最优组合 |
| **M2 模型侧实验** | M1 胜出配置上做 use_priors on/off 对照(object 场景优先) | priors 有正收益才启用 |
| **M3 后处理实验** | P1 scale 截断 + P2 top_n + P3 alpha,在 M0 指标上回归 | 每项单独验证不伤指标 |
| **M4 固化** | 胜出参数写回 runner/config(流程结构零改动) | 全指标卡对比 ≥ 基线 |

## 4. 风险与约束

- **6GB 显存**:一次只跑一个 GPU 任务(黑屏教训)。留出评估渲染用小尺寸(如 640²,gsplat 已够)。
- **SfM 位姿噪声 → priors 反效果**:实验决定,失败即回退 pose-free(官方数据已表明 DL3DV 类场景收益≈0)。
- **离线环境**:评估只用 PSNR/SSIM 手写实现 + gsplat 渲染,**不引入需下载权重的包**。
- 每次实验用同一批输入图(锁定测试集),保证可比性。

## 5. 需要用户提供/确认

1. 一个典型测试场景(最好是用户觉得"不够清晰/不完整"的现有任务,可直接复用其上传图)。
2. 确认 M0 先行(先建评估基线再动任何参数)。
