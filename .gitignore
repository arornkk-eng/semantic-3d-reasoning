# ===================== Python / 工具链缓存 =====================
__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# ===================== 虚拟环境 =====================
venv/
.venv/
venv-open3d/

# ===================== 运行时数据（自动生成，不入库） =====================
data/
results/

# 评估报告保留，体积较大的可再生产物不入库
evaluation/**/*.ply
evaluation/**/*.splat
evaluation/**/gaussians.pt
evaluation/**/raw_gaussians.pt

# ===================== 编辑器 / OS =====================
.idea/
.vscode/
.claude/
.workbuddy/
.DS_Store

# ===================== 密钥 / 本地配置 =====================
.env

# ===================== 模型权重 =====================
models/

# ===================== 外部项目生成物与示例资产 =====================
supersplat-editor/node_modules/
supersplat-editor/dist/
ZipSplat-main/assets/
third_party/sam2/

# 嵌套发布仓库(独立 git 管理,不入父仓库)
semantic-3d-editor-release/

# LaTeX 编译产物
paper_arxiv/*.aux
paper_arxiv/*.bbl
paper_arxiv/*.blg
paper_arxiv/*.log
paper_arxiv/*.out

# 运行日志
evaluation/**/*.log
evaluation/**/*.log.err
/backend-*.log
/logs/

# 本地评估与浏览器冒烟产物
/evaluation/open3d-tsdf/
/evaluation/pycolmap/
/evaluation/realtime-detection/
/evaluation/semantic-labels/
/evaluation/segmentation/browser/*/

# 前端构建调试映射（生产运行不需要）
/frontend/public/**/*.map

# 本地下载的检测权重
/yolo*.pt
